{
  "name": "prueba-monorepo",
  "version": "1.0.0",
  "description": "Monorepo para aplicación distribuida con React y Node.js",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "gateway"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:gateway\" \"npm run dev:frontend\" \"npm run dev:admin\" \"npm run dev:medico\"",
    "dev:gateway": "cd gateway && npm run dev",
    "dev:frontend": "cd apps/frontend && npm run dev",
    "dev:admin": "cd apps/admin-service && npm run dev",
    "dev:medico": "cd apps/medico-service && npm run dev",
    "build": "npm run build:packages && npm run build:apps",
    "build:packages": "npm run build --workspace=packages/shared",
    "build:apps": "npm run build --workspace=apps/frontend && npm run build --workspace=apps/admin-service && npm run build --workspace=apps/medico-service",
    "install:all": "npm install && npm run install:packages && npm run install:apps",
    "install:packages": "npm install --workspace=packages/shared",
    "install:apps": "npm install --workspace=apps/frontend && npm install --workspace=apps/admin-service && npm install --workspace=apps/medico-service"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
